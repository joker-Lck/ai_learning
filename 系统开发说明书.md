# 基于多智能体的个性化学习资源生成系统

## 系统开发说明书

---

> **项目名称**：基于多智能体的个性化学习资源生成系统  
> **参赛赛事**：中国软件杯  
> **版本**：v7.2  
> **技术栈**：FastAPI + Next.js 14 + MySQL 8.0 + 讯飞星火 AI  

---

## 目录

- [第一部分 需求分析](#第一部分-需求分析)
  - [1.1 项目背景与问题提出](#11-项目背景与问题提出)
  - [1.2 新时代大学生学习需求调研](#12-新时代大学生学习需求调研)
  - [1.3 现有方案不足分析](#13-现有方案不足分析)
  - [1.4 需求规格定义](#14-需求规格定义)
  - [1.5 AI 技术与需求的结合点](#15-ai-技术与需求的结合点)
- [第二部分 系统设计](#第二部分-系统设计)
  - [2.1 系统总体架构](#21-系统总体架构)
  - [2.2 多智能体协作架构](#22-多智能体协作架构)
  - [2.3 数据库设计](#23-数据库设计)
  - [2.4 API 接口设计](#24-api-接口设计)
  - [2.5 用户界面设计](#25-用户界面设计)
- [第三部分 核心算法](#第三部分-核心算法)
  - [3.1 AI 模型路由与调用算法](#31-ai-模型路由与调用算法)
  - [3.2 混合检索算法（ANN + KNN + RRF 融合）](#32-混合检索算法ann--knn--rrf-融合)
    - [3.2.5 高级检索方法（2023-2026 新型算法）](#325-高级检索方法2023-2026-新型算法)
  - [3.3 文本向量化算法](#33-文本向量化算法)
  - [3.4 三级回退检索策略](#34-三级回退检索策略)
  - [3.5 关键词 Jaccard 相似度算法](#35-关键词-jaccard-相似度算法)
  - [3.6 防幻觉交叉验证算法](#36-防幻觉交叉验证算法)
  - [3.7 多层缓存淘汰算法](#37-多层缓存淘汰算法)
  - [3.8 容错 JSON 解析算法](#38-容错-json-解析算法)
  - [3.9 图片压缩算法](#39-图片压缩算法)
- [第四部分 技术实现](#第四部分-技术实现)
  - [4.1 学生画像智能体](#41-学生画像智能体)
  - [4.2 资源生成智能体](#42-资源生成智能体)
  - [4.3 学习路径智能体](#43-学习路径智能体)
  - [4.4 智能辅导智能体](#44-智能辅导智能体)
  - [4.5 效果评估智能体](#45-效果评估智能体)
  - [4.6 文件智能识别与导入](#46-文件智能识别与导入)
  - [4.7 内容安全服务](#47-内容安全服务)
  - [4.8 高级检索服务](#48-高级检索服务)
- [第五部分 前端实现与用户体验](#第五部分-前端实现与用户体验)
  - [5.1 技术选型与架构](#51-技术选型与架构)
  - [5.2 六大功能模块](#52-六大功能模块)
  - [5.3 视觉设计与交互动效](#53-视觉设计与交互动效)
  - [5.4 用户体验提升策略](#54-用户体验提升策略)
- [第六部分 系统集成与部署](#第六部分-系统集成与部署)
  - [6.1 开发环境与工具链](#61-开发环境与工具链)
  - [6.2 数据库初始化与迁移](#62-数据库初始化与迁移)
  - [6.3 环境检查与一键启动](#63-环境检查与一键启动)
- [第七部分 测试与质量保障](#第七部分-测试与质量保障)
  - [7.1 测试策略](#71-测试策略)
  - [7.2 API 接口测试](#72-api-接口测试)
  - [7.3 AI 输出质量保障](#73-ai-输出质量保障)
  - [7.4 兼容性与性能测试](#74-兼容性与性能测试)
- [第八部分 创新实践与亮点总结](#第八部分-创新实践与亮点总结)
  - [8.1 核心创新点](#81-核心创新点)
  - [8.2 技术难点与解决方案](#82-技术难点与解决方案)
  - [8.3 项目价值与展望](#83-项目价值与展望)

---

# 第一部分 需求分析

## 1.1 项目背景与问题提出

### 1.1.1 政策背景

教育部《教育信息化2.0行动计划》明确提出"以信息化引领构建以学习者为中心的全新教育生态"。《新一代人工智能发展规划》将"智能教育"列为重点应用，要求利用 AI 技术实现因材施教、个性化学习。2024年以来，大语言模型（LLM）技术的成熟为教育领域的智能化应用提供了前所未有的技术基础。

### 1.1.2 现实痛点

当前大学生在自主学习过程中面临以下核心困境：

| 痛点 | 表现 | 影响 |
|------|------|------|
| **资源碎片化** | 学习资料散落在教材、网课、笔记等多个渠道 | 学习效率低，知识体系不完整 |
| **缺乏个性化** | 通用资源无法适配个人知识基础和认知风格 | 学习效果因人而异，薄弱环节得不到针对性强化 |
| **学习路径盲目** | 不知道先学什么、后学什么，缺乏系统规划 | 知识断层，前后置依赖关系混乱 |
| **反馈滞后** | 考试后才知道薄弱点，缺乏过程性评估 | 错失最佳干预时机 |
| **答疑不便** | 课后遇到问题难以及时获得专业解答 | 问题积累，学习信心下降 |

## 1.2 新时代大学生学习需求调研

### 1.2.1 需求调研方法

- **问卷调查**：面向 200+ 名在校大学生，覆盖计算机科学、软件工程、数据科学等专业
- **深度访谈**：与 15 名不同年级学生进行一对一对话
- **文献分析**：参考国内外 30+ 篇教育技术领域论文
- **竞品分析**：对市面 10+ 款学习工具进行功能对比

### 1.2.2 核心需求提炼

基于调研，提炼出 **6 大核心需求**：

```
┌─────────────────────────────────────────────────────────┐
│                    大学生学习需求模型                      │
├─────────────┬─────────────┬─────────────┬───────────────┤
│   个性化画像  │  智能资源生成  │  路径规划    │   实时辅导    │
│  (Know Me)   │  (Teach Me)  │ (Guide Me)  │  (Help Me)   │
├─────────────┼─────────────┼─────────────┼───────────────┤
│   效果评估    │  知识管理    │             │              │
│ (Evaluate Me)│ (Organize Me)│             │              │
└─────────────┴─────────────┴─────────────┴───────────────┘
```

| 需求编号 | 需求名称 | 需求描述 | 优先级 |
|---------|---------|---------|--------|
| R1 | 个性化学生画像 | 通过对话和数据采集，构建包含认知风格、知识基础、学习目标等 8 维度的学生画像 | P0 |
| R2 | 多类型资源生成 | 根据学习主题自动生成讲解文档、思维导图、练习题、视频、动画、代码案例、拓展阅读 7 种类型资源 | P0 |
| R3 | 智能学习路径 | 基于画像和学习目标，规划包含前置知识依赖的有序学习路径 | P0 |
| R4 | 实时智能辅导 | 支持多轮对话答疑，提供文字解答 + 图解说明 + 实例代码的多模态回答 | P0 |
| R5 | 学习效果评估 | 基于真实学习数据（成绩、错题、学习计划）生成多维度评估报告 | P1 |
| R6 | 知识库管理 | 支持上传课程资料，构建个人 RAG 知识库，辅助精准检索和问答 | P1 |

### 1.2.3 用户画像

| 用户类型 | 特征 | 核心诉求 |
|---------|------|---------|
| **自主学习型** | 主动性强，有明确学习目标 | 高效路径规划 + 深度资源生成 |
| **查漏补缺型** | 有薄弱科目，需要针对性强化 | 错题管理 + 个性化评估 + 专项练习 |
| **考前突击型** | 时间紧迫，需要快速掌握重点 | 智能学习计划 + 重点资源推荐 |

## 1.3 现有方案不足分析

| 对比维度 | 传统学习平台 | 通用 AI 助手 | 本系统 |
|---------|-------------|-------------|--------|
| 个性化程度 | 低（通用推荐） | 中（对话理解） | **高（8 维度画像 + 多源数据）** |
| 资源类型 | 单一（文档/视频） | 单一（文本） | **7 种多模态资源** |
| 学习路径 | 无 | 无 | **AI 规划 + 进度跟踪** |
| 数据驱动 | 弱 | 无 | **成绩/错题/课程表全量数据** |
| 知识检索 | 全文搜索 | 无 | **RAG 向量 + 关键词混合检索** |
| 安全机制 | 基础 | 无 | **8 类敏感词 + 防幻觉 + 事实核查** |

## 1.4 需求规格定义

### 1.4.1 功能需求

**FR-01 学生画像构建**
- 系统应支持通过 8 轮对话采集学生信息（知识基础、认知风格、学习目标、薄弱环节、学习历史、兴趣领域、资源偏好、专业信息）
- 系统应支持手动编辑画像各维度
- 系统应自动从课程表、成绩、错题等数据中提取画像特征
- 画像数据应支持版本管理，记录历史变更

**FR-02 多类型资源生成**
- 系统应支持 7 种资源类型的 AI 生成：讲解文档、思维导图、练习题、视频、SVG 动画、代码案例、拓展阅读
- 每种资源应支持独立的生成参数配置（主题、难度、数量等）
- 生成的资源应通过内容安全检查
- 资源应支持 Markdown/PDF 格式导出

**FR-03 学习路径规划**
- 系统应基于学生画像和学习目标生成有序学习路径
- 路径应包含知识点之间的前置依赖关系
- 系统应支持学习进度跟踪和完成率统计
- 支持周计划、备考计划、自定义计划三种模式

**FR-04 智能辅导**
- 系统应支持多轮对话式答疑
- 首次进入辅导模块时，AI 主动发送欢迎引导消息
- 回答应包含文字解答、图解说明（Mermaid 语法）、实例代码三个维度
- 系统应基于学生认知风格个性化调整回答方式
- 支持 SSE 流式输出，实时展示生成过程
- 集成记忆增强：自动检索用户历史记忆构建上下文

**FR-05 学习效果评估**
- 系统应基于真实数据（成绩、课程、错题、学习计划）生成评估报告
- 评估应覆盖知识掌握、学习态度、薄弱环节、进步空间 4 个维度
- 应提供可视化图表（柱状图、进度条）展示评估结果
- 应支持 AI 生成和数据驱动两种评估模式（含降级方案）

**FR-06 学生数据管理**
- 课程表：支持手动录入、编辑、删除，支持文件导入（PDF/Word/Excel/PPT/图片）
- 成绩管理：支持录入和文件导入，按学期/课程统计
- 错题本：支持添加、标记掌握状态、删除，支持文件导入
- 学习计划：支持 AI 生成和手动创建
- 文件导入时 AI 优先校验内容类型，不匹配直接报错提示
- AI 识别期间切换 Tab 不中断识别进程

**FR-07 文件智能识别**
- 系统应支持上传图片（JPG/PNG/BMP/WEBP）、PDF、Excel、CSV 文件
- 图片文件应通过 OCR 提取文字，失败则降级到多模态 AI 视觉识别
- PDF 文件应通过 PyPDF2 提取文本，扫描版 PDF 应通过 PyMuPDF 转图片后 OCR 识别
- Excel/CSV 文件应通过结构化解析提取数据
- 识别结果应支持预览确认后才保存入库

**FR-08 RAG 知识库**
- 支持上传 TXT/MD/DOC/DOCX/PDF/PPT/PPTX/XLS/XLSX/CSV 等格式文档
- 文档应自动解析、分段、生成向量嵌入并入库
- 检索应采用向量 + 关键词混合检索 + RRF 融合排序
- 检索结果应集成到辅导问答中，提供知识引用

### 1.4.2 非功能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| NFR-01 | 响应时间 | API 首字节响应 ≤ 3s（流式场景），完整响应 ≤ 90s |
| NFR-02 | 可用性 | 系统应提供降级方案，AI 服务不可用时仍能展示数据 |
| NFR-03 | 安全性 | JWT 认证 + API 限流（120次/分钟）+ 内容安全过滤 |
| NFR-04 | 兼容性 | 支持 Chrome/Edge/Firefox 最新两个版本 |
| NFR-05 | 可扩展性 | 智能体架构支持独立扩展，新增 Agent 不影响已有功能 |
| NFR-06 | 数据持久化 | 所有用户数据持久化存储，支持版本管理 |

## 1.5 AI 技术与需求的结合点

```
┌──────────────────────────────────────────────────────────────┐
│                    AI 技术融合应用全景图                        │
├──────────────────┬───────────────────────────────────────────┤
│   大语言模型(LLM)  │  画像构建 · 资源生成 · 路径规划 · 辅导答疑  │
│   讯飞星火 generalv3.5/4.0Ultra  │  效果评估 · 文档分析 · 文件识别             │
├──────────────────┼───────────────────────────────────────────┤
│   多模态视觉模型    │  课程表图片识别 · 成绩单图片识别             │
│   32k-vision      │  手写笔记识别 · 错题图片解析                 │
├──────────────────┼───────────────────────────────────────────┤
│   向量嵌入 + RAG   │  知识库语义检索 · 上下文增强问答             │
│   768d Embedding  │  引用标注 · 事实核查 · 防幻觉               │
├──────────────────┼───────────────────────────────────────────┤
│   推理模型         │  复杂逻辑推理 · 多步分析 · 结构化输出        │
│   k2.x Reasoning  │  JSON 生成 · 知识图谱构建                  │
└──────────────────┴───────────────────────────────────────────┘
```

**核心技术结合策略**：

1. **模型分级路由**：根据任务复杂度自动选择最优模型——简单任务用 generalv3.5（快速低成本），复杂推理用 4.0Ultra（高质量），图片识别用 generalv3.5（多模态）
2. **Prompt Engineering**：针对每种资源类型设计专用 Prompt 模板，通过结构化 JSON 输出确保 AI 返回可解析的数据格式
3. **RAG 增强生成**：将用户上传的课程资料转化为向量知识库，在问答和资源生成时检索相关上下文，提升回答的准确性和针对性
4. **多智能体协作**：通过事件驱动的消息总线实现 Agent 之间的信息共享和任务协调，支持并行执行和异步评估

---

# 第二部分 系统设计

## 2.1 系统总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层 (User Layer)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 学生画像  │ │ 资源生成  │ │ 学习路径  │ │ 智能辅导  │ ...       │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘           │
│       └────────────┼────────────┼────────────┘                  │
├────────────────────┼────────────┼────────────────────────────────┤
│                    ▼            ▼                                │
│              ┌─────────────────────┐                            │
│              │   Next.js 14 前端    │  SSR + Client Components   │
│              │   Tailwind CSS      │  Zustand 状态管理           │
│              │   Framer Motion     │  Turbopack 构建             │
│              └──────────┬──────────┘                            │
├─────────────────────────┼───────────────────────────────────────┤
│                         ▼                                       │
│              ┌─────────────────────┐                            │
│              │   FastAPI 后端       │  RESTful + SSE 流式         │
│              │   JWT 认证 + 限流    │  Pydantic 校验              │
│              └──────────┬──────────┘                            │
├─────────────────────────┼───────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              智能体协作层 (Agent Layer)                     │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐               │  │
│  │  │ 协调器    │  │ 消息总线  │  │ 消息协议  │               │  │
│  │  │Coordinator│  │MessageBus│  │  Protocol │               │  │
│  │  └────┬─────┘  └────┬─────┘  └──────────┘               │  │
│  │       │             │                                     │  │
│  │  ┌────┴───┐   ┌─────┴────┐   ┌─────────┐   ┌─────────┐ │  │
│  │  │画像Agent│   │资源Agent │   │路径Agent │   │辅导Agent│ │  │
│  │  └────────┘   └──────────┘   └─────────┘   └─────────┘ │  │
│  │       ┌───────────┐   ┌───────────┐                     │  │
│  │       │评估Agent   │   │QA Service │                     │  │
│  │       └───────────┘   └───────────┘                     │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              AI 能力层 (AI Layer)                          │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │            讯飞星火 API Client                        │ │  │
│  │  │  generalv3.5 (简单) │ 4.0Ultra (复杂) │ vision (图片) │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐  │  │
│  │  │ 向量嵌入服务   │  │ RAG 知识库    │  │ 内容安全服务   │  │  │
│  │  │768d Embedding │  │FAISS+关键词   │  │ 敏感词+防幻觉  │  │  │
│  │  └──────────────┘  └──────────────┘  └───────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              数据层 (Data Layer)                           │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │  │
│  │  │ai_auth │ │profiles│ │resources│ │ai_paths│ │ai_tutor│ │  │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ │  │
│  │  ┌────────────┐ ┌────────┐ ┌──────────────┐             │  │
│  │  │assessments │ │agents  │ │rag_knowledge │             │  │
│  │  └────────────┘ └────────┘ └──────────────┘             │  │
│  │              MySQL 8.0 (8 库 24 表)                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

**架构设计原则**：

- **分层解耦**：用户层、智能体层、AI 能力层、数据层四层分离，各层通过标准接口通信
- **智能体自治**：每个 Agent 独立负责一个业务领域，拥有自己的 Prompt 模板和降级策略
- **事件驱动**：Agent 之间通过消息总线异步通信，支持并行执行和松耦合协作
- **降级容错**：每层都有降级方案——AI 失败用数据驱动降级，向量检索失败用关键词降级

## 2.2 多智能体协作架构

### 2.2.1 智能体角色定义

| Agent | 角色 | 职责 | 使用模型 |
|-------|------|------|---------|
| ProfileAgent | 画像师 | 8 维度对话式画像构建、版本管理、动态更新 | generalv3.5 |
| ResourceAgent | 讲师 | 7 种类型学习资源生成、安全检查、RAG 同步 | generalv3.5/4.0Ultra |
| PathAgent | 导师 | 学习路径规划、进度跟踪、智能计划生成 | generalv3.5 |
| TutorAgent | 辅导员 | 多轮对话答疑、多模态回答、个性化解答 | generalv3.5/4.0Ultra |
| AssessmentAgent | 评估师 | 多维度学习效果评估、数据驱动分析 | 4.0Ultra |
| Coordinator | 指挥官 | 任务分发、Agent 协调、结果聚合 | — |

### 2.2.2 消息协议

系统定义了 13 种消息类型和 6 种 Agent 角色，通过统一的 `AgentMessage` 协议实现 Agent 间通信：

```
AgentMessage {
    id: UUID
    type: MessageType
        REQUEST / RESPONSE / NOTIFICATION / QUERY /
        COMMAND / FEEDBACK / ERROR / HEARTBEAT /
        TASK_ASSIGN / TASK_COMPLETE / STATUS_UPDATE /
        COLLABORATION_REQUEST / COLLABORATION_RESPONSE
    source: AgentRole   (PROFILE / RESOURCE / PATH / TUTOR / ASSESSMENT / COORDINATOR)
    target: AgentRole
    payload: Dict
    correlation_id: UUID   ← 关联请求-响应对
    priority: int
}
```

### 2.2.3 协作流程——综合学习计划

```
用户请求 ──► Coordinator
                │
                ├──► ProfileAgent.get_or_build_profile()  ──┐
                ├──► StudentDataService.get_student_data()  ──┤ 并行采集
                │                                              │
                ▼                                              ▼
            协商优化 (基于画像 + 学生数据综合分析)
                │
        ┌───────┴───────┐
        ▼               ▼
  ResourceAgent    PathAgent           并行生成
  (生成资源)       (规划路径)
        │               │
        ▼               ▼
  AssessmentAgent ──► 异步评估           结果聚合 ──► 返回用户
```

### 2.2.4 事件驱动消息总线

`MessageBus` 提供四种通信模式：

| 模式 | 说明 | 使用场景 |
|------|------|---------|
| Pub/Sub | 发布-订阅，一对多 | 画像更新通知所有 Agent |
| Point-to-Point | 点对点，一对一 | Coordinator → 特定 Agent |
| Request/Response | 请求-响应，带关联 ID | 同步调用等待结果 |
| Broadcast | 广播，忽略来源 | 系统级通知 |

消息总线使用线程池并发处理消息，避免阻塞主线程。

## 2.3 数据库设计

### 2.3.1 数据库架构

系统采用 **8 个独立数据库**，按业务领域隔离：

| 数据库 | 表数 | 用途 |
|--------|------|------|
| **ai_auth** | 2 | 用户认证与会话管理 |
| **ai_profiles** | 5 | 学生画像、课程表、成绩、错题、学习计划 |
| **ai_resources** | 2 | 学习资源管理、安全日志 |
| **ai_paths** | 2 | 学习路径、路径进度 |
| **ai_tutor** | 3 | 辅导会话、消息、知识引用 |
| **ai_assessments** | 3 | 评估记录、评估维度、学习活动 |
| **ai_agents** | 2 | 智能体协作日志、任务记录 |
| **ai_rag_knowledge** | 5 | 知识文档、知识点、分类、关联、使用日志 |

### 2.3.2 核心表结构

**student_profiles（学生画像表）**

```sql
CREATE TABLE student_profiles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    profile_data JSON NOT NULL,    -- 8 维度画像数据
    conversation_log JSON,          -- 对话历史
    version INT DEFAULT 1,          -- 版本号
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user (user_id)
);
```

画像 JSON 结构：
```json
{
    "major": "计算机科学与技术",
    "grade_level": "大三",
    "knowledge_base": {"level": "中等", "topics": ["数据结构", "算法"]},
    "cognitive_style": "视觉型学习者",
    "learning_goals": ["考研 408", "掌握机器学习"],
    "weak_points": ["操作系统", "计算机网络"],
    "interest_areas": ["人工智能", "后端开发"],
    "preferred_resources": ["视频教程", "代码示例"]
}
```

**learning_assessments（学习评估表）**

```sql
CREATE TABLE learning_assessments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    assessment_type ENUM('weekly','monthly','custom','comprehensive','auto_generated'),
    assessment_data JSON NOT NULL,
    overall_score DECIMAL(5,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**knowledge_documents（RAG 知识文档表）**

```sql
CREATE TABLE knowledge_documents (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    subject VARCHAR(100),
    file_type VARCHAR(20),
    content_text MEDIUMTEXT,
    embedding JSON,                   -- 768 维向量嵌入
    FULLTEXT INDEX ft_content (content_text)
);
```

### 2.3.3 ER 关系图

```
users (1) ──────── (N) student_profiles
users (1) ──────── (N) course_schedules
users (1) ──────── (N) student_grades
users (1) ──────── (N) error_notes
users (1) ──────── (N) study_plans
users (1) ──────── (N) learning_resources
users (1) ──────── (N) learning_paths
learning_paths (1) ── (N) path_progress
users (1) ──────── (N) tutor_sessions
tutor_sessions (1) ── (N) tutor_messages
tutor_messages (1) ── (N) tutor_knowledge_refs
users (1) ──────── (N) learning_assessments
learning_assessments (1) ── (N) assessment_dimensions
knowledge_documents (1) ── (N) knowledge_points
knowledge_documents (N) ── (N) document_categories
```

### 2.3.4 记忆系统数据库（ai_memory）

系统实现了无限长时记忆架构，包含 8 张核心表：

```sql
-- 1. 短期记忆：Token 级上下文窗口
short_term_memory (user_id, session_id, role, content, token_count)

-- 2. 情景记忆：对话事件/场景
episodic_memory (user_id, episode_type, title, summary, content, importance)

-- 3. 语义记忆：SPO 三元组事实知识
semantic_memory (user_id, fact_type, subject, predicate, object, confidence)

-- 4. 实体记忆：KV 画像存储
entity_memory (user_id, entity_type, entity_name, attributes, description)

-- 5. 实体关系：知识图谱
entity_relations (source_entity_id, target_entity_id, relation_type, weight)

-- 6. 记忆元数据：遗忘机制控制
memory_metadata (memory_type, memory_id, importance, decay_rate, is_forgotten)

-- 7. 记忆冲突：冲突检测与修正
memory_conflicts (conflict_type, old_memory_id, new_memory_id, resolution_strategy)

-- 8. 访问日志：遗忘曲线分析
memory_access_log (memory_type, memory_id, access_type)
```

**记忆类型层次**：

```
短期记忆 (Token 窗口)
├── 全部保存，最近 50 条
├── 超出自动清理
└── 用于上下文连续性

长期记忆
├── 情景记忆 (Episodic)
│   ├── 对话事件、学习场景
│   └── 按重要性+时间衰减
├── 语义记忆 (Semantic)
│   ├── SPO 三元组事实
│   ├── 置信度评分
│   └── 冲突检测与修正
└── 实体记忆 (Entity)
    ├── KV 画像属性
    ├── 实体关系图谱
    └── RAG 精确查找
```

**遗忘机制**：基于艾宾浩斯遗忘曲线 `R = e^(-t/S)`，其中 R 为保留率，t 为距上次访问天数，S 为稳定性（与访问次数正相关）。重要性低于阈值 0.2 的记忆自动标记为遗忘。

**冲突修正**：当新增事实与已有事实矛盾时，系统自动检测并记录冲突，支持三种解决策略：保留旧记忆、保留新记忆、合并验证。

**代码架构**（Handler 分层设计）：

```
MemoryDB (基类)
├── _execute() / _fetchone() / _fetchall() / _count()
│
├── ShortTermHandler    — Token 级上下文窗口
├── EpisodicHandler     — 情景记忆 CRUD + 搜索
├── SemanticHandler     — SPO 三元组 + 冲突检测
├── EntityHandler       — KV 画像 + 知识图谱 BFS
├── ForgettingHandler   — 遗忘曲线 + 元数据 + 强化
└── ConflictHandler     — 冲突查询 + 三种解决策略

MemoryService (门面类)
└── 组合 6 个 Handler，对外暴露统一 API
```

**源文件**：`services/memory_service.py` → MemoryDB / 6 个 Handler / MemoryService

## 2.4 API 接口设计

### 2.4.1 接口规范

- **协议**：RESTful HTTP + SSE（Server-Sent Events）流式
- **认证**：Bearer Token（JWT HS256，24h 有效期）
- **限流**：全局 120 次/分钟，登录 10 次/分钟，注册 5 次/分钟
- **响应格式**：

```json
{
    "success": true,
    "message": "操作成功",
    "data": { ... },
    "error": null
}
```

### 2.4.2 接口分类汇总（共 50+ 端点）

| 模块 | 端点数 | 关键接口 |
|------|--------|---------|
| 认证 `/api/auth` | 5 | login, register, me, change-password, guest |
| 画像 `/api/agent` | 3 | build-profile, get-profile, update-profile-field |
| 资源 | 2 | generate-resources, export-resource |
| 路径 | 2 | plan-path, update-path-progress |
| 辅导 | 1 | tutor |
| 评估 | 2 | assess, comprehensive-plan |
| 学生数据 | 12 | courses/grades/errors/plans CRUD |
| 文件导入 | 3 | import-courses/grades/errors-from-file |
| RAG | 3 | upload-to-rag, rag-documents, analyze-documents |
| **高级检索** | **6** | **advanced-search, hyde-search, multi-query-search, rag-fusion-search, graph-search, contextual-upload** |
| 流式 `/api/stream` | 9 | SSE 资源生成/辅导/安全检查/事实验证 |
| 系统 | 2 | health, api-info |

### 2.4.3 高级检索 API 详解

**源文件**：`backend/api/agent.py`

| 端点 | 方法 | 功能 | 参数 |
|------|------|------|------|
| `/api/agent/advanced-search` | POST | 统一检索入口 | query, strategy, subject, limit |
| `/api/agent/hyde-search` | POST | HyDE 假设性文档嵌入 | query, subject, model, limit |
| `/api/agent/multi-query-search` | POST | 多查询检索 | query, subject, num_variants, limit |
| `/api/agent/rag-fusion-search` | POST | RAG-Fusion + RRF | query, subject, num_variants, rrf_k, limit |
| `/api/agent/graph-search` | POST | 图谱增强检索 | query, subject, graph_depth, limit |
| `/api/agent/contextual-upload` | POST | 上下文分块上传 | files, subject |

**统一检索入口 strategy 参数说明**：

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| `auto` | 自动选择（短查询用 HyDE，长查询用 RAG-Fusion） | 默认推荐 |
| `hyde` | 假设性文档嵌入 | 短查询、概念性问题 |
| `multi_query` | 多查询检索 | 提高召回率 |
| `rag_fusion` | RAG-Fusion + RRF | 通用场景（推荐） |
| `contextual` | 上下文精排 | 需要高精度时 |
| `graph` | 图谱增强检索 | 有图谱数据时 |
| `hybrid` | HyDE + RAG-Fusion 组合 | 平衡速度与精度 |
| `ensemble` | 全方法集成 | 最全面（耗时较长） |

**请求示例**：

```json
POST /api/agent/advanced-search
Authorization: Bearer <token>
{
    "query": "梯度下降原理",
    "strategy": "rag_fusion",
    "subject": "机器学习",
    "limit": 5
}
```

**响应示例**：

```json
{
    "success": true,
    "message": "检索完成，返回 5 条结果",
    "data": {
        "results": [
            {
                "id": 1,
                "title": "梯度下降法详解",
                "subject": "机器学习",
                "content_text": "...",
                "similarity": 0.85,
                "retrieval_method": "rag_fusion",
                "rrf_score": 0.032
            }
        ],
        "strategy_used": "rag_fusion",
        "count": 5
    }
}
```

## 2.5 用户界面设计

### 2.5.1 设计理念

- **暗色玻璃态**：深蓝背景 `#060d1f` + 毛玻璃卡片 `backdrop-blur-xl` + 微透明边框 `bg-white/[0.04]`
- **渐变色彩**：青色 `#64ffda` 主色调，配合蓝、紫、琥珀、翠绿等辅助色
- **科技感**：Canvas 3D 光球背景（13 个）+ Framer Motion 流畅动画
- **信息密度**：卡片化布局，每屏展示最大有效信息

### 2.5.2 页面结构

```
┌──────────────────────────────────────────────────┐
│  Section 0: Hero 首页                             │
│  ┌─────────────────────────────────────────────┐ │
│  │  品牌展示 + 统计概览 + 快捷入口按钮           │ │
│  │  [学习看板] [功能选择]                        │ │
│  └─────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────┤
│  Section 1: 工作台                                │
│  ┌─────────────────────────────────────────────┐ │
│  │  用户数据仪表盘（内部可滚动）                 │ │
│  └─────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────┤
│  Section 2: 功能选择/模块内容                      │
│  ┌──────────┐  ┌───────────────────────────────┐ │
│  │  侧边栏   │  │        主内容区                │ │
│  │          │  │                               │ │
│  │ 🎯 画像  │  │   根据选中模块动态渲染           │ │
│  │ 📚 资源  │  │                               │ │
│  │ 🗺️ 路径  │  │   ProfileModule               │ │
│  │ 💬 辅导  │  │   ResourcesModule              │ │
│  │ 📊 评估  │  │   PathModule                   │ │
│  │ 📖 知识库 │  │   TutorModule                  │ │
│  │          │  │   AssessmentModule              │ │
│  │ ─────── │  │   RagKnowledgeModule            │ │
│  │ 用户信息  │  │                               │ │
│  └──────────┘  └───────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

单页面导航：通过 URL 参数 `?module=xxx` 切换模块，无页面跳转，状态保持。

**快捷入口**：Hero 页面提供"学习看板"和"功能选择"两个快捷按钮，方便用户快速进入对应界面。

---

# 第三部分 核心算法

## 3.1 AI 模型路由与调用算法

### 3.1.1 讯飞星火模型配置

**源文件**：`services/spark_client.py` → `SparkClient` 类

系统使用讯飞星火 Spark-X2-Flash 模型，支持推理内容提取：

```python
SPARK_MODEL = "spark-x"  # Spark-X2-Flash 推理模型
```

**API 端点**：

| 功能 | 端点 | 认证 |
|------|------|------|
| 文本对话 | `spark-api-open.xf-yun.com/agent/v1` | `api_key:api_secret` |
| 图片生成 (TTI) | `spark-api.cn-huabei-1.xf-yun.com/v2.1/tti` | 独立凭证 |
| OCR 识别 | `cbm01.cn-huabei-1.xf-yun.com/v1/private/se75ocrbm` | 独立凭证 |
| 语音合成 (TTS) | `api-dx.xf-yun.com/v1/private/dts_create` | 独立凭证 |

### 3.1.2 推理模型内容提取

Spark-X2-Flash 是推理模型，回答分为两部分：
- `reasoning_content`：推理过程（思考链）
- `content`：最终答案

**提取策略**：
1. 优先使用 `content`（最终答案）
2. 若 `content` 为空或过短（<20字符），从 `reasoning_content` 提取
3. 提取优先级：JSON → 定义性句子 → 过滤推理过程后的文本

```python
# 定义性关键词优先提取
definition_keywords = ['是', '指', '用于', '属于', '一种', '方法', '技术', '算法']

# 过滤推理过程行
reasoning_starters = ('嗯，', '首先', '其次', '接下来', '我需要', '让我', '现在')
```

## 3.2 混合检索算法（ANN + KNN + RRF 融合）

**源文件**：`data/rag_knowledge_base.py` → `RAGKnowledgeBase` 类

系统采用三路混合检索策略，兼顾语义相似度和关键词精确匹配：

### 3.2.1 KNN 关键词检索

基于 MySQL 全文索引的精确关键词匹配：

```python
# MySQL FULLTEXT 搜索
SELECT * FROM knowledge_points
WHERE MATCH(content) AGAINST(%s IN NATURAL LANGUAGE MODE)
```

- **优势**：精确匹配专业术语、公式、代码
- **适用**：用户查询包含明确关键词时

### 3.2.2 ANN 向量检索

基于余弦相似度的语义向量检索：

```python
# 余弦相似度计算
def cosine_similarity(vec_a, vec_b):
    dot = np.dot(vec_a, vec_b)
    norm = np.linalg.norm(vec_a) * np.linalg.norm(vec_b)
    return dot / norm if norm > 0 else 0.0
```

- **优势**：理解语义相近的表达（如"梯度下降"≈"优化算法"）
- **适用**：用户使用自然语言描述问题时

### 3.2.3 RRF 融合排序

Reciprocal Rank Fusion 将多路检索结果融合：

```python
def rrf_fusion(results_list, k=60):
    """RRF 融合排序"""
    scores = {}
    for results in results_list:
        for rank, item in enumerate(results):
            doc_id = item['id']
            scores[doc_id] = scores.get(doc_id, 0) + 1 / (k + rank + 1)
    return sorted(scores.items(), key=lambda x: x[1], reverse=True)
```

- **参数 k=60**：平滑因子，平衡高排名和低排名结果的贡献
- **融合策略**：KNN 结果 + ANN 结果 → RRF 统一排序 → Top-N 返回

### 3.2.4 检索流程

```
用户查询
  ├── KNN 关键词检索 ──→ 关键词结果集
  └── ANN 向量检索   ──→ 语义结果集
         │
         ▼
    RRF 融合排序
         │
         ▼
    Top-N 相关文档
         │
         ▼
    注入 LLM 上下文
```

### 3.2.5 高级检索方法（2023-2026 新型算法）

**源文件**：`services/advanced_retrieval_service.py` → `AdvancedRetrievalService` 类

系统在基础混合检索之上，实现了 5 种 2023-2026 年前沿检索方法，通过统一入口 `smart_search()` 按策略路由：

#### (1) HyDE — 假设性文档嵌入（Gao et al., 2023）

**核心思想**：查询通常是短句，与文档在向量空间存在语义鸿沟。HyDE 让 LLM 先根据查询生成一个"假想的理想答案文档"，再用该文档的向量去检索，弥合语义鸿沟。

```
用户问题 → LLM 生成假设答案(200-400字) → 向量化假设答案 → FAISS 检索
```

- **优势**：不改变索引结构，仅改变查询端，零侵入
- **适用**：短查询、概念性问题

#### (2) Multi-Query — 多查询检索（LangChain, 2023）

**核心思想**：同一个问题可以有多种表述方式。LLM 将原始查询改写为 3-5 个语义等价但措辞不同的变体，分别检索后合并去重。

```
"梯度下降原理" →
  "梯度下降法的工作机制是什么"
  "梯度下降如何优化损失函数"
  "梯度下降的数学推导过程"
→ 三次检索 → 合并去重
```

- **优势**：提高召回率，捕捉单一查询遗漏的文档

#### (3) RAG-Fusion + RRF — 查询融合（Raudaschl, 2023）

**核心思想**：在 Multi-Query 基础上，不简单合并结果，而是使用 Reciprocal Rank Fusion (RRF) 算法对多个排名列表进行加权融合。

```
RRF Score(d) = Σ 1/(k + rank_i(d))    k=60（平滑常数）
```

- **优势**：比简单合并更鲁棒，是当前 RAG 社区最佳实践
- **推荐**：系统默认高级检索策略

#### (4) Contextual Retrieval — 上下文检索（Anthropic, 2024）

**核心思想**：Anthropic 2024 年提出。传统分块会丢失上下文，导致 chunk 嵌入语义不完整。在嵌入前，用 LLM 为每个 chunk 生成上下文前缀。

```
原始 chunk: "反向传播通过链式法则计算梯度"
添加上下文后: "[来自《机器学习》第5章·神经网络训练] 反向传播通过链式法则计算梯度"
```

- **优势**：Anthropic 报告检索失败率降低 35%
- **实现**：`add_contextual_document()` 方法，入库时自动添加上下文前缀

#### (5) Graph-Enhanced RAG — 图谱增强检索（Microsoft GraphRAG, 2024）

**核心思想**：利用已有的实体记忆图谱，从查询中提取实体，在图谱中遍历 1-2 跳关联实体，用关联实体扩展查询后再检索。

```
"梯度下降" → 图谱查找 → [学习率, 损失函数, 梯度爆炸, Adam优化器]
→ 扩展查询检索 → 结合图谱关系给出结构化答案
```

- **优势**：充分利用已有图谱数据，提供可解释的关联推理
- **集成**：智能辅导模块自动使用图谱增强检索

#### 统一检索入口与策略路由

```python
# services/advanced_retrieval_service.py
class AdvancedRetrievalService:
    def smart_search(self, user_id, query, subject, limit, strategy):
        """
        strategy 策略选项：
          - "auto"       — 自动选择（短查询用 HyDE，长查询用 RAG-Fusion）
          - "hyde"       — 假设性文档嵌入
          - "multi_query" — 多查询检索
          - "rag_fusion"  — RAG-Fusion + RRF（推荐）
          - "contextual"  — 上下文精排
          - "graph"       — 图谱增强
          - "hybrid"      — HyDE + RAG-Fusion 组合
          - "ensemble"    — 全方法集成（取并集，最全面）
        """
```

#### 高级检索流程总览

```
用户查询
  │
  ├─ HyDE ──────→ LLM生成假设文档 → 向量化 → FAISS检索
  │
  ├─ Multi-Query → LLM生成N个变体 → 分别检索 → 去重合并
  │
  ├─ RAG-Fusion ─→ LLM生成N个变体 → 分别检索 → RRF融合排序
  │
  ├─ Contextual ─→ 向量粗召回(top20) → LLM上下文评分 → 精排
  │
  └─ Graph ──────→ LLM提取实体 → 图谱遍历 → 扩展查询 → 检索
         │
         ▼
    统一 RRF 融合（hybrid/ensemble 模式）
         │
         ▼
    最终检索结果（含 retrieval_method 标记）
```

## 3.3 无限长时记忆架构

**源文件**：`services/tutor_agent.py` → `TutorAgent` 类（集成记忆增强）

### 3.3.1 记忆分层模型

系统实现四层记忆架构，模拟人类记忆机制，集成在辅导智能体中：

| 记忆层 | 存储方式 | 生命周期 | 容量 |
|--------|---------|---------|------|
| 短期记忆 | MySQL `short_term_memory` | 会话内，自动清理 | 最近 50 条 |
| 情景记忆 | MySQL `episodic_memory` | 长期，按重要性衰减 | 无限 |
| 语义记忆 | MySQL `semantic_memory` | 长期，SPO 三元组 | 无限 |
| 实体记忆 | MySQL `entity_memory` + `entity_relations` | 长期，KV + 图谱 | 无限 |

### 3.3.2 遗忘机制（艾宾浩斯曲线）

基于遗忘曲线公式 `R = e^(-t/S)` 实现智能衰减：

- **R**：保留率（0~1）
- **t**：距上次访问的天数
- **S**：稳定性 = max(1, access_count × 2)

```python
stability = max(1, mem['access_count'] * 2)
retention = np.exp(-days_since_access / stability)
new_importance = mem['importance'] * retention
```

- 访问次数越多 → 稳定性越高 → 衰减越慢
- 重要性低于阈值 0.2 的记忆自动标记为遗忘
- 每次访问自动强化记忆（importance += 0.015）

### 3.3.3 冲突检测与修正

当新增事实与已有事实矛盾时，系统自动检测并记录冲突：

```python
# 检测：同一 subject + predicate 但不同 object
SELECT id, object, confidence FROM semantic_memory
WHERE user_id = %s AND subject = %s AND predicate = %s AND object != %s
```

三种解决策略：
- **keep_old**：保留旧记忆，删除新记忆
- **keep_new**：保留新记忆，删除旧记忆
- **merge**：两者保留，标记为已验证

### 3.3.4 记忆增强问答

智能辅导模块集成记忆系统，实现个性化问答：

```
用户提问
  ├── 检索语义记忆（相关事实）
  ├── 检索情景记忆（历史对话）
  ├── 检索实体记忆（知识图谱）
  ├── 获取用户知识背景
  └── 获取短期对话上下文
         │
         ▼
    构建增强 Prompt
         │
         ▼
    AI 生成回答
         │
         ▼
    提取并保存新记忆
```

### 3.3.5 代码架构（集成在 TutorAgent）

记忆功能直接集成在辅导智能体中，无需独立服务：

```
TutorAgent (辅导智能体)
├── answer_query()           — 记忆增强问答（主入口）
├── _retrieve_memories()     — 检索语义/情景/实体记忆
├── _build_user_context()    — 构建用户知识背景
├── _build_enhanced_context()— 构建增强 Prompt
├── _extract_and_save_memories() — 提取并保存新记忆
├── get_user_knowledge_map() — 知识图谱查询
├── get_learning_recommendations() — 学习推荐
└── apply_memory_maintenance() — 遗忘曲线维护
```

## 3.4 防幻觉机制

**源文件**：`services/antihallucination_service.py`

三层防护确保 AI 回答的可靠性：

1. **RAG 优先检索**：回答前先检索知识库，有相关内容则引用
2. **事实核查验证**：对 AI 生成的关键事实进行交叉验证
3. **引用标注溯源**：标注信息来源，便于用户验证

```python
# 置信度评分
confidence = rag_similarity * 0.6 + fact_check_score * 0.4
verified = confidence >= 0.7
```

## 3.7 性能优化算法

### 3.7.1 numpy 向量化暴力搜索

**源文件**：`data/rag_knowledge_base.py` → `_brute_force_vector_search()`

当 FAISS 不可用时，使用 numpy 矩阵批量计算余弦相似度：

```python
# 向量化计算（替代逐条循环）
query_vec = np.array(query_embedding, dtype='float32')
doc_matrix = np.array(embeddings_list, dtype='float32')

query_norm = np.linalg.norm(query_vec)
doc_norms = np.linalg.norm(doc_matrix, axis=1)
similarities = np.dot(doc_matrix, query_vec) / (doc_norms * query_norm)

# 使用 argsort 获取 Top-K
top_indices = np.argsort(similarities)[::-1][:limit]
```

- **优化前**：逐条计算余弦相似度，O(n) 次 Python 循环
- **优化后**：numpy 矩阵运算，单次向量化计算
- **性能提升**：10-100 倍

### 3.7.2 AC 自动机敏感词匹配

**源文件**：`services/content_safety_service.py` → `AhoCorasick` 类

使用 AC 自动机算法实现高效的多模式字符串匹配：

```python
class AhoCorasick:
    def __init__(self):
        self.goto = [{}]      # 转移函数
        self.fail = [0]       # 失败指针
        self.output = [[]]    # 输出函数
    
    def add_pattern(self, pattern: str, pattern_id: int):
        """添加模式串"""
        state = 0
        for char in pattern:
            if char not in self.goto[state]:
                self.goto[state][char] = self.state_count
                # ...
            state = self.goto[state][char]
        self.output[state].append((pattern_id, pattern))
    
    def search(self, text: str) -> List[Tuple[int, str, int]]:
        """搜索文本中的所有模式匹配，O(n) 复杂度"""
```

- **优化前**：遍历敏感词列表，逐个检查是否存在于文本中，O(m*n)
- **优化后**：AC 自动机一次扫描，O(n) 复杂度
- **敏感词数量**：80+ 个，覆盖 8 大类违规内容

### 3.7.3 LRU Cache 线程安全缓存

**源文件**：`data/rag_knowledge_base.py` → `LRUCache` 类

使用 `collections.OrderedDict` 实现 LRU 缓存：

```python
class LRUCache:
    def __init__(self, max_size: int = 200, ttl: int = 600):
        self._cache = OrderedDict()
        self._max_size = max_size
        self._ttl = ttl
        self._lock = threading.Lock()
    
    def get(self, key: str):
        """获取缓存值，过期或不存在返回None"""
        with self._lock:
            if key in self._cache:
                value, timestamp = self._cache[key]
                if time.time() - timestamp < self._ttl:
                    self._cache.move_to_end(key)  # LRU: 移到末尾
                    return value
                else:
                    del self._cache[key]  # TTL 过期
            return None
    
    def set(self, key: str, value):
        """设置缓存值"""
        with self._lock:
            if key in self._cache:
                del self._cache[key]
            elif len(self._cache) >= self._max_size:
                self._cache.popitem(last=False)  # LRU: 淘汰最旧
            self._cache[key] = (value, time.time())
```

- **优化前**：字典 + 时间戳，手动查找最旧条目淘汰，O(n)
- **优化后**：OrderedDict 自动维护访问顺序，O(1) 淘汰
- **线程安全**：使用 `threading.Lock` 保护所有操作

### 3.7.4 前端性能优化

**源文件**：`frontend/next.config.js`、`frontend/components/`

| 优化项 | 方法 | 效果 |
|--------|------|------|
| **构建优化** | 代码分割、CSS 优化、包导入优化 | 构建体积减小 |
| **渲染优化** | React.memo、useCallback | 减少不必要重渲染 |
| **布局优化** | CSS contain: strict | 减少重绘范围 |
| **动画优化** | will-change: transform | GPU 加速动画 |
| **加载优化** | DNS 预获取、字体预加载 | 减少网络延迟 |

### 3.7.5 前端企业级安全

**源文件**：`frontend/next.config.js`、`frontend/middleware.ts`、`frontend/lib/api.ts`

#### 安全头配置

通过 `next.config.js` 的 `async headers()` 为所有响应注入安全头：

| 安全头 | 值 | 作用 |
|--------|-----|------|
| X-Content-Type-Options | nosniff | 防止 MIME 嗅探攻击 |
| X-Frame-Options | DENY | 防止 Clickjacking |
| X-XSS-Protection | 1; mode=block | 浏览器级 XSS 防护 |
| Referrer-Policy | strict-origin-when-cross-origin | 控制 Referer 泄露 |
| Permissions-Policy | camera=(), microphone=() | 限制浏览器 API |
| Content-Security-Policy | 自定义策略 | 限制脚本/样式/连接来源 |
| Strict-Transport-Security | max-age=31536000 | 强制 HTTPS |

#### API 客户端可靠性

| 特性 | 说明 |
|------|------|
| **自动重试** | 5xx/网络错误自动重试2次，指数退避（1s → 2s → 4s） |
| **超时分级** | 普通请求 30s / 文件上传 60s / AI 生成 120s / 流式 180s |
| **统一错误处理** | 401 自动跳转登录，消除重复代码 |
| **null body 检查** | SSE 流读取前检查 response.body |

#### XSS 防护

MarkdownRenderer 使用 `rehype-sanitize` 插件过滤恶意 HTML，防止存储型 XSS。

#### Turbopack 兼容

开发环境使用 Turbopack 加速，`webpack` 配置仅在生产构建时生效，避免兼容性警告。

---

# 第四部分 技术实现

## 4.8 高级检索服务

**源文件**：`services/advanced_retrieval_service.py` → `AdvancedRetrievalService` 类

### 4.8.1 服务概述

高级检索服务实现了 5 种 2023-2026 年前沿检索方法，通过统一入口 `smart_search()` 按策略路由，为 RAG 知识库提供多种检索能力。

### 4.8.2 核心方法

| 方法 | 函数 | 论文来源 | 核心思想 |
|------|------|----------|----------|
| HyDE | `hyde_search()` | Gao et al., 2023 | LLM 生成假设答案，用答案向量检索 |
| Multi-Query | `multi_query_search()` | LangChain, 2023 | LLM 生成多个查询变体，分别检索合并 |
| RAG-Fusion | `rag_fusion_search()` | Raudaschl, 2023 | 多查询 + RRF 倒数排名融合 |
| Contextual | `contextual_search()` | Anthropic, 2024 | 向量粗召回 + LLM 上下文评分精排 |
| Graph-Enhanced | `graph_enhanced_search()` | Microsoft GraphRAG, 2024 | 实体提取 + 图谱遍历 + 查询扩展 |

### 4.8.3 统一检索入口

```python
class AdvancedRetrievalService:
    def smart_search(self, user_id, query, subject, limit, strategy):
        """
        智能检索入口：根据策略选择最佳检索方法
        
        参数：
          strategy:
            - "auto"       — 自动选择（短查询用 HyDE，长查询用 RAG-Fusion）
            - "hyde"       — 假设性文档嵌入
            - "multi_query" — 多查询检索
            - "rag_fusion"  — RAG-Fusion + RRF（推荐）
            - "contextual"  — 上下文精排
            - "graph"       — 图谱增强
            - "hybrid"      — HyDE + RAG-Fusion 组合
            - "ensemble"    — 全方法集成（取并集，最全面）
        """
```

### 4.8.4 HyDE 实现细节

```python
def hyde_search(self, query, subject, limit, model):
    """HyDE 检索：LLM 生成假设答案 → 向量化 → 检索"""
    # 1. 生成假设文档
    hypothetical_doc = self._generate_hypothetical_document(query, subject, model)
    
    # 2. 向量化假设文档
    doc_embedding = self.embedding_service.get_embedding(hypothetical_doc)
    
    # 3. 使用假设文档向量检索
    results = self.rag_kb.search_documents_by_vector(doc_embedding, limit=limit)
    
    return results

def _generate_hypothetical_document(self, query, subject, model):
    """用 LLM 生成假设性答案文档"""
    prompt = f"请针对以下问题，写一段详细的教学内容作为参考答案。要求：200-400字..."
    return self.qa_service.call_simple(prompt, max_tokens=600)
```

### 4.8.5 RAG-Fusion + RRF 实现细节

```python
def rag_fusion_search(self, query, subject, limit, num_variants, rrf_k):
    """RAG-Fusion：多查询 + Reciprocal Rank Fusion 排序"""
    # 1. 生成查询变体
    variants = self._generate_query_variants(query, num_variants)
    all_queries = [query] + variants
    
    # 2. 分别检索
    ranked_lists = []
    for q in all_queries:
        q_embedding = self.embedding_service.get_embedding(q)
        hits = self.rag_kb.search_documents_by_vector(q_embedding, limit=limit * 3)
        ranked_lists.append(hits)
    
    # 3. RRF 融合排序
    rrf_scores = {}
    for ranked in ranked_lists:
        for rank, doc in enumerate(ranked):
            doc_id = doc.get('id')
            rrf_scores[doc_id] = rrf_scores.get(doc_id, 0) + 1.0 / (rrf_k + rank + 1)
    
    # 4. 按 RRF 分数排序返回
    sorted_ids = sorted(rrf_scores.keys(), key=lambda x: rrf_scores[x], reverse=True)
    return [doc_data_map[did] for did in sorted_ids[:limit]]
```

### 4.8.6 Graph-Enhanced RAG 实现细节

```python
def graph_enhanced_search(self, user_id, query, subject, limit, graph_depth):
    """图谱增强检索：实体识别 → 图谱遍历 → 查询扩展 → 融合检索"""
    # 1. 从查询中提取实体
    entities = self._extract_entities(query)
    
    # 2. 在知识图谱中遍历关联实体
    graph_context = self._traverse_graph(user_id, entities, graph_depth)
    
    # 3. 用关联实体扩展查询
    expanded_query = self._expand_query_with_graph(query, graph_context)
    
    # 4. 使用扩展查询检索
    expanded_embedding = self.embedding_service.get_embedding(expanded_query)
    results = self.rag_kb.search_documents_by_vector(expanded_embedding, limit=limit)
    
    return results
```

### 4.8.7 智能辅导集成

智能辅导模块自动使用图谱增强检索，从用户知识图谱中提取关联实体扩展查询：

```python
# services/tutor_agent.py
def _retrieve_memories(self, ms, user_id, question, subject):
    """检索相关记忆（集成高级检索）"""
    memories = {'semantic': [], 'episodic': [], 'entity': [], 'rag_docs': []}
    
    # 原有记忆检索...
    
    # 高级 RAG 检索（Graph-Enhanced）
    from services.advanced_retrieval_service import retrieval_service
    rag_results = retrieval_service.graph_enhanced_search(
        user_id=user_id, query=question, subject=subject, limit=3
    )
    memories['rag_docs'] = rag_results
    
    return memories
```

### 4.8.8 上下文分块入库

参考 Anthropic Contextual Retrieval (2024)，为每个段落添加上下文前缀后再嵌入：

```python
def add_contextual_document(self, title, subject, content_text, ...):
    """上下文分块入库：为每个段落添加上下文前缀后再嵌入"""
    # 1. 分段
    paragraphs = self._split_paragraphs(content_text)
    
    # 2. 为每段生成上下文前缀
    contextualized = self._add_context_to_paragraphs(title, subject, paragraphs)
    
    # 3. 拼接后向量化入库
    full_text = '\n\n'.join(contextualized)
    embedding = self.embedding_service.get_embedding(full_text[:4000])
    
    doc_id = self.rag_kb.add_document(
        title=title, subject=subject,
        content_text=full_text, embedding=embedding, ...
    )
    return doc_id
```

**上下文前缀示例**：

```
原始段落: "反向传播通过链式法则计算梯度"
添加上下文后: "[来自《机器学习》第5章·神经网络训练] 反向传播通过链式法则计算梯度"
```