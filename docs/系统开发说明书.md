# 基于多智能体的个性化学习资源生成系统

## 系统开发说明书

---

> **项目名称**：基于多智能体的个性化学习资源生成系统    
> **版本**：v1.0  

> **技术栈**：FastAPI + Next.js 14 + SQLite + MiMo AI  

---

## 目录

- [第一部分 需求分析](#第一部分-需求分析)
  - [1.1 项目背景与问题提出](#11-项目背景与问题提出)
  - [1.2 需求调研与用户画像](#12-需求调研与用户画像)
  - [1.3 现有方案对比](#13-现有方案对比)
  - [1.4 需求规格](#14-需求规格)
- [第二部分 系统设计](#第二部分-系统设计)
  - [2.1 系统总体架构](#21-系统总体架构)
  - [2.2 多智能体协作架构](#22-多智能体协作架构)
  - [2.3 数据库设计](#23-数据库设计)
  - [2.4 API 接口设计](#24-api-接口设计)
  - [2.5 用户界面设计](#25-用户界面设计)
- [第三部分 核心算法](#第三部分-核心算法)
  - [3.1 AI 模型路由](#31-ai-模型路由)
  - [3.2 混合检索系统](#32-混合检索系统)
  - [3.3 高级检索方法]
  - [3.4 无限长时记忆架构](#34-无限长时记忆架构)
  - [3.5 防幻觉机制](#35-防幻觉机制)
  - [3.6 性能优化算法](#36-性能优化算法)
  - [3.7 数据访问层 (DAO)](#37-数据访问层-dao)
  - [3.8 自定义异常体系](#38-自定义异常体系)
- [第四部分 前端工程](#第四部分-前端工程)
  - [4.1 技术选型与架构](#41-技术选型与架构)
  - [4.2 安全与性能](#42-安全与性能)
- [第五部分 企业级特性](#第五部分-企业级特性)
  - [5.1 安全](#51-安全)
  - [5.2 可观测性](#52-可观测性)
  - [5.3 容器化](#53-容器化)
  - [5.4 启动方式](#54-启动方式)
- [附录](#附录)
  - [附录A：技术栈清单](#附录a技术栈清单)
  - [附录B：API接口完整列表](#附录bapi接口完整列表)
  - [附录C：数据库表结构](#附录c数据库表结构)
  - [附录D：环境变量配置](#附录d环境变量配置)

---

# 第一部分 需求分析

## 1.1 项目背景与问题提出

### 政策背景

教育部《教育信息化2.0行动计划》明确提出"以信息化引领构建以学习者为中心的全新教育生态"。《新一代人工智能发展规划》将"智能教育"列为重点应用，要求利用 AI 技术实现因材施教、个性化学习。2024年以来，大语言模型（LLM）技术的成熟为教育领域的智能化应用提供了前所未有的技术基础。

### 现实痛点

| 痛点 | 表现 | 影响 |
|------|------|------|
| **资源碎片化** | 学习资料散落在教材、网课、笔记等多个渠道 | 学习效率低，知识体系不完整 |
| **缺乏个性化** | 通用资源无法适配个人知识基础和认知风格 | 薄弱环节得不到针对性强化 |
| **学习路径盲目** | 不知道先学什么、后学什么，缺乏系统规划 | 知识断层，前后置依赖关系混乱 |
| **反馈滞后** | 考试后才知道薄弱点，缺乏过程性评估 | 错失最佳干预时机 |
| **答疑不便** | 课后遇到问题难以及时获得专业解答 | 问题积累，学习信心下降 |

## 1.2 需求调研与用户画像

### 调研方法

- **问卷调查**：200+ 名在校大学生，覆盖计算机科学、软件工程、数据科学等专业
- **深度访谈**：15 名不同年级学生一对一对话
- **文献分析**：30+ 篇教育技术领域论文
- **竞品分析**：10+ 款学习工具功能对比

### 核心需求

| 编号 | 需求 | 描述 | 优先级 |
|------|------|------|--------|
| R1 | 个性化学生画像 | 对话式构建 9 维度动态画像 | P0 |
| R2 | 多类型资源生成 | 7 种类型：文档/思维导图/题库/视频/动画/代码/阅读 | P0 |
| R3 | 智能学习路径 | 基于画像规划有序学习路径 | P0 |
| R4 | 实时智能辅导 | 多轮对话 + 多模态回答 | P0 |
| R5 | 学习效果评估 | 基于真实数据的多维度评估 | P1 |
| R6 | 知识库管理 | 上传资料构建 RAG 知识库 | P1 |

### 用户画像

| 类型 | 特征 | 核心诉求 |
|------|------|---------|
| **自主学习型** | 主动性强，有明确目标 | 高效路径 + 深度资源 |
| **查漏补缺型** | 有薄弱科目 | 错题管理 + 专项练习 |
| **考前突击型** | 时间紧迫 | 学习计划 + 重点推荐 |

## 1.3 现有方案对比

| 维度 | 传统平台 | 通用 AI 助手 | **本系统** |
|------|---------|-------------|----------|
| 个性化 | 低 | 中 | **高（9 维度画像）** |
| 资源类型 | 单一 | 单一 | **7 种多模态** |
| 学习路径 | 无 | 无 | **AI 规划** |
| 数据驱动 | 弱 | 无 | **全量数据** |
| 知识检索 | 全文搜索 | 无 | **RAG + 关键词混合** |
| 安全机制 | 基础 | 无 | **敏感词(外部配置) + 防幻觉 + DAO层** |

## 1.4 需求规格

### 功能需求

| 编号 | 功能 | 核心要求 |
|------|------|---------|
| FR-01 | 学生画像 | 8 轮对话采集，支持手动编辑，自动从数据提取特征 |
| FR-02 | 资源生成 | 7 种类型 AI 生成，独立参数配置，内容安全检查 |
| FR-03 | 路径规划 | 前置依赖关系，进度跟踪，三种计划模式 |
| FR-04 | 智能辅导 | 多轮对话，Mermaid 图解，SSE 流式，记忆增强 |
| FR-05 | 效果评估 | 4 维度评估，可视化图表，降级方案 |
| FR-06 | 数据管理 | 课程表/成绩/错题/计划 CRUD，文件导入 |
| FR-07 | 文件识别 | OCR + AI 视觉，扫描版 PDF，结构化解析 |
| FR-08 | RAG 知识库 | 多格式上传，向量 + 关键词混合检索 |

### 非功能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| NFR-01 | 响应时间 | 流式首字节 ≤ 3s，完整响应 ≤ 90s |
| NFR-02 | 可用性 | AI 不可用时仍能展示数据 |
| NFR-03 | 安全性 | JWT(启动校验) + 限流 + Pydantic校验 + 内容过滤(外部配置) |
| NFR-04 | 兼容性 | Chrome/Edge/Firefox 最新版 |
| NFR-05 | 可扩展性 | 智能体独立扩展 |

---

# 第二部分 系统设计

## 2.1 系统总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层 (User Layer)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 学生画像  │ │ 资源生成  │ │ 学习路径  │ │ 智能辅导  │ ...       │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘           │
├───────┴────────────┴────────────┴────────────┴───────────────────┤
│                    ▼            ▼                                │
│              ┌─────────────────────┐                            │
│              │   Next.js 12 前端    │  Pages Router          │
│              │   Tailwind CSS      │  Zustand 状态管理           │
│              └──────────┬──────────┘                            │
├─────────────────────────┼───────────────────────────────────────┤
│                         ▼                                       │
│              ┌─────────────────────┐                            │
│              │   FastAPI 后端       │  RESTful + SSE 流式         │
│              │   JWT 认证 + 限流    │  Pydantic 校验              │
│              └──────────┬──────────┘                            │
├─────────────────────────┼───────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              智能体协作层 (Agent Layer) — 6个专业智能体      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐               │  │
│  │  │ 协调器    │  │ 消息总线  │  │ 消息协议  │               │  │
│  │  └────┬─────┘  └────┬─────┘  └──────────┘               │  │
│  │  ┌────┴───┐   ┌─────┴────┐   ┌─────────┐   ┌─────────┐ │  │
│  │  │画像Agent│   │资源Agent │   │路径Agent │   │辅导Agent│ │  │
│  │  └────────┘   └──────────┘   └─────────┘   └─────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              AI 能力层 (AI Layer)                          │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐  │  │
│  │  │ MiMo LLM  │  │ RAG 知识库    │  │ 内容安全服务   │  │  │
│  │  │ + 向量嵌入    │  │ FAISS+关键词  │  │ AC自动机+防幻觉│  │  │
│  │  └──────────────┘  └──────────────┘  └───────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                         ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              数据层 (Data Layer) — SQLite (9个.db文件)    │  │
│  │  auth │ profiles │ resources │ paths │ tutor │ memory    │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

**设计原则**：

- **分层解耦**：四层分离，标准接口通信
- **智能体自治**：每个 Agent 独立负责一个业务领域
- **事件驱动**：消息总线异步通信，支持并行执行
- **降级容错**：每层都有降级方案
- **数据访问层**：DAO 模式封装数据库操作，统一连接管理

## 2.2 多智能体协作架构

### 智能体角色

| Agent | 角色 | 职责 | 模型 |
|-------|------|------|------|
| ProfileAgent | 画像师 | 9 维度对话式画像构建 | mimo-v2.5-pro |
| ResourceAgent | 讲师 | 7 种类型资源生成 | mimo-v2.5-pro |
| PathAgent | 导师 | 学习路径规划 | mimo-v2.5-pro |
| TutorAgent | 辅导员 | 多轮对话答疑 + 记忆增强 | mimo-v2.5-pro |
| AssessmentAgent | 评估师 | 多维度学习效果评估 | mimo-v2.5-pro |
| Coordinator | 指挥官 | 任务分发、结果聚合 | — |

### 协作流程

```
用户请求 ──► Coordinator
                │
                ├──► ProfileAgent  ──┐
                ├──► StudentData    ──┤ 并行采集
                ▼                    ▼
            协商优化 (画像 + 数据综合分析)
                │
        ┌───────┴───────┐
        ▼               ▼
  ResourceAgent    PathAgent       并行生成
        │               │
        ▼               ▼
  AssessmentAgent ──► 异步评估   ──► 返回用户
```

## 2.3 数据库设计

### 数据库架构（9 个独立 SQLite .db 文件）

> 系统已从 SQLite 迁移到 SQLite，9个独立 .db 文件存放在 `data/databases/` 目录。SQLite 内置于 Python（`sqlite3` 模块），无需安装额外数据库服务。使用 `PRAGMA journal_mode=WAL` + `PRAGMA foreign_keys=ON`。

| 数据库文件 | 用途 | 核心表 |
|-----------|------|--------|
| **ai_auth.db** | 认证 | users, sessions |
| **ai_profiles.db** | 画像 | student_profiles, course_schedules, grades, error_notes, study_plans |
| **ai_resources.db** | 资源 | learning_resources, safety_logs |
| **ai_paths.db** | 路径 | learning_paths, path_progress |
| **ai_tutor.db** | 辅导 | tutor_sessions, messages, knowledge_refs |
| **ai_assessments.db** | 评估 | assessments, dimensions, activities |
| **ai_agents.db** | 协作 | collaboration_logs, tasks |
| **ai_rag_knowledge.db** | 知识库 | documents, points, categories（FTS5全文索引） |
| **ai_memory.db** | 记忆 | 8 张表（见 3.4 节） |

### 核心表结构

```sql
-- 学生画像（9 维度 JSON，SQLite 用 TEXT 存储）
student_profiles (id, user_id, profile_data TEXT, version, updated_at)

-- 学习评估
learning_assessments (id, user_id, assessment_type, assessment_data TEXT, overall_score)

-- RAG 知识文档（FTS5 全文索引）
knowledge_documents (id, title, subject, content_text, embedding TEXT, FTS5 virtual table)
```

## 2.4 API 接口设计

### 接口规范

- **协议**：RESTful HTTP + SSE 流式
- **认证**：Bearer Token（JWT HS256，24h 有效期），启动时强制校验密钥
- **限流**：全局 120 次/分钟，AI 生成端点独立限流
- **请求校验**：全部端点使用 Pydantic 模型验证，拒绝非法参数
- **异常处理**：自定义异常类分层捕获，业务异常与系统异常分离

### 接口汇总（50+ 端点）

| 模块 | 端点数 | 关键接口 |
|------|--------|---------|
| 认证 `/api/auth` | 5 | login, register, me, guest |
| 画像 | 3 | build-profile, get-profile, update |
| 资源 | 2 | generate-resources, export |
| 路径 | 2 | plan-path, update-progress |
| 辅导 | 1 | tutor |
| 评估 | 2 | assess, comprehensive-plan |
| 学生数据 | 12 | courses/grades/errors CRUD |
| 文件导入 | 3 | import-courses/grades/errors |
| RAG | 3 | upload, list, analyze |
| **高级检索** | **6** | **advanced-search, hyde, multi-query, rag-fusion, graph, contextual** |
| 流式 | 9 | SSE 资源/辅导/安全 |
| 系统 | 2 | health, info |

### 高级检索 API

| 端点 | 功能 |
|------|------|
| `POST /api/agent/advanced-search` | 统一入口（支持 8 种策略） |
| `POST /api/agent/hyde-search` | HyDE 假设性文档嵌入 |
| `POST /api/agent/multi-query-search` | 多查询检索 |
| `POST /api/agent/rag-fusion-search` | RAG-Fusion + RRF |
| `POST /api/agent/graph-search` | 图谱增强检索 |
| `POST /api/agent/contextual-upload` | 上下文分块上传 |

## 2.5 用户界面设计

### 设计理念

- **暗色玻璃态**：深蓝背景 + 毛玻璃卡片 + 微透明边框
- **渐变色彩**：青色 `#64ffda` 主色调
- **科技感**：Canvas 3D 光球 + Framer Motion 动画

### 页面结构

```
Section 0: Hero 首页 — 品牌展示 + 快捷入口
Section 1: 工作台 — 用户数据仪表盘
Section 2: 功能模块 — 6 大模块动态渲染
```

单页面导航：URL 参数 `?module=xxx` 切换，无跳转，状态保持。

---

# 第三部分 核心算法

## 3.1 AI 模型路由

**源文件**：`services/spark_client.py`

| 功能 | 端点 | 认证 |
|------|------|------|
| 文本对话 | `spark-api-open.xf-yun.com/agent/v1` | api_key:api_secret |
| 图片生成 | `spark-api.cn-huabei-1.xf-yun.com/v2.1/tti` | 独立凭证 |
| OCR | `cbm01.cn-huabei-1.xf-yun.com/v1/private/se75ocrbm` | 独立凭证 |

MiMo-v2.5-pro 是推理模型，回答分两部分：
- `reasoning_content`：推理过程（思考链）
- `content`：最终答案

**提取策略**：优先 content → 空则从 reasoning_content 提取 → JSON → 定义性句子

## 3.2 混合检索系统

**源文件**：`data/rag_knowledge_base.py`

系统采用三路混合检索，兼顾语义相似度和关键词精确匹配：

### 3.2.1 KNN 关键词检索

SQLite FTS5 全文索引精确匹配，适合专业术语、公式、代码。使用 `CREATE VIRTUAL TABLE ... USING fts5(tokenize='unicode61')` 创建，配合同步触发器维护索引。

### 3.2.2 ANN 向量检索

FAISS `IndexFlatIP` 内积搜索（归一化后等价余弦相似度），理解语义相近表达。向量维度 768（MiMo Embedding API (mimo-embedding)）。

### 3.2.3 RRF 融合排序

```python
RRF_score(d) = Σ 1/(k + rank_i(d))    # k=60
```

KNN 结果 + ANN 结果 → RRF 统一排序 → Top-N 返回。

### 3.2.4 三级回退策略

| 级别 | 触发条件 | 检索方式 | 响应时间 |
|------|---------|---------|---------|
| L1 | FAISS 就绪 | IndexFlatIP 搜索 | ~5ms |
| L2 | FAISS 可用但索引为空 | 惰性构建后搜索 | ~500ms（首次） |
| L3 | FAISS 不可用 | 暴力余弦 KNN | ~100ms |

## 3.3 高级检索方法（2023-2026）

**源文件**：`services/advanced_retrieval_service.py`

系统实现了 11 种前沿检索策略，通过 `smart_search()` 统一入口按策略路由：

### (1) HyDE — 假设性文档嵌入（Gao et al., 2023）

```
用户问题 → LLM 生成假设答案(200-400字) → 向量化 → FAISS 检索
```

- **优势**：零侵入，仅改变查询端
- **适用**：短查询、概念性问题

### (2) Multi-Query — 多查询检索（LangChain, 2023）

```
"梯度下降原理" → LLM 改写为 3-5 个变体 → 分别检索 → 合并去重
```

- **优势**：提高召回率，捕捉单一查询遗漏的文档

### (3) RAG-Fusion + RRF（Raudaschl, 2023）

多查询 + Reciprocal Rank Fusion 加权融合，是当前 RAG 社区最佳实践。

### (4) Contextual Retrieval（Anthropic, 2024）

为每个 chunk 生成上下文前缀后再嵌入，检索失败率降低 35%。

```
原始: "反向传播通过链式法则计算梯度"
添加上下文后: "[来自《机器学习》第5章·神经网络训练] 反向传播通过链式法则计算梯度"
```

### (5) Graph-Enhanced RAG（Microsoft GraphRAG, 2024）

```
"梯度下降" → 图谱查找 → [学习率, 损失函数, 梯度爆炸]
→ 扩展查询检索 → 结合图谱关系给出结构化答案
```

### 策略路由（11 种）

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| `auto` | 自动选择（短查询用 HyDE，长查询用 RAG-Fusion） | 默认 |
| `knn` | KNN 关键词检索（SQLite FTS5 精确匹配） | 专业术语、公式 |
| `ann` | ANN 向量检索（FAISS 语义匹配） | 模糊语义查询 |
| `hybrid` | KNN + ANN + RRF 混合（基座策略） | 通用推荐 |
| `hyde` | 假设性文档嵌入（2023） | 短查询、概念性问题 |
| `multi_query` | 多查询检索（2023） | 提高召回率 |
| `rag_fusion` | RAG-Fusion + RRF（2023） | 通用推荐 |
| `contextual` | 上下文精排（2024） | 高精度场景 |
| `graph` | 图谱增强检索（2024） | 有图谱数据时 |
| `hybrid_advl` | 基座 + HyDE + RAG-Fusion 三路 RRF | 平衡速度与精度 |
| `ensemble` | 全部 6 种方法取并集，RRF 融合 | 最全面 |

## 3.4 无限长时记忆架构

**源文件**：`services/memory_service.py`

### 四层记忆模型

| 层 | 存储 | 生命周期 | 容量 |
|----|------|---------|------|
| 短期记忆 | `short_term_memory` | 会话内 | 最近 50 条 |
| 情景记忆 | `episodic_memory` | 长期衰减 | 无限 |
| 语义记忆 | `semantic_memory` | 长期 | 无限 |
| 实体记忆 | `entity_memory` + `entity_relations` | 长期 | 无限 |

### 遗忘机制（艾宾浩斯曲线）

```
R = e^(-t/S)
R: 保留率, t: 距上次访问天数, S: 稳定性 = max(1, access_count × 2)
```

访问越多 → 稳定性越高 → 衰减越慢。重要性 < 0.2 自动标记遗忘。

### 记忆增强问答

```
用户提问 → 检索语义/情景/实体记忆 → 构建增强 Prompt → AI 回答 → 保存新记忆
```

## 3.5 防幻觉机制

**源文件**：`services/content_safety_service.py`

三层防护：

1. **RAG 优先检索**：回答前先检索知识库
2. **事实核查验证**：交叉验证关键事实
3. **引用标注溯源**：标注信息来源

```
confidence = rag_similarity × 0.6 + fact_check_score × 0.4
verified = confidence ≥ 0.7
```

## 3.6 性能优化算法

### 3.6.1 numpy 向量化暴力搜索

**源文件**：`data/rag_knowledge_base.py`

暴力搜索使用 numpy 矩阵批量计算，性能提升 10-100 倍。

### 3.6.2 AC 自动机敏感词匹配

**源文件**：`services/content_safety_service.py` + `config/sensitive_words.json`

120+ 敏感词（支持外部 JSON 配置热更新），AC 自动机 O(n) 复杂度一次扫描，替代逐词遍历 O(m×n)。

### 3.6.3 LRU Cache 线程安全缓存

**源文件**：`data/rag_knowledge_base.py`

`OrderedDict` 实现，TTL 过期 + LRU 淘汰，线程安全。

### 3.6.4 前端性能优化

| 优化项 | 方法 | 效果 |
|--------|------|------|
| 构建 | 代码分割、包导入优化 | 体积减小 |
| 渲染 | React.memo、useCallback | 减少重渲染 |
| 布局 | CSS contain: strict | 减少重绘 |
| 动画 | will-change: transform | GPU 加速 |

### 3.6.5 前端企业级安全

| 安全头 | 作用 |
|--------|------|
| X-Content-Type-Options: nosniff | 防 MIME 嗅探 |
| X-Frame-Options: DENY | 防 Clickjacking |
| Content-Security-Policy | 限制脚本/样式来源 |
| Strict-Transport-Security | 强制 HTTPS |

API 客户端：自动重试（指数退避）、超时分级（30s/60s/120s）、统一错误处理、rehype-sanitize XSS 防护。

---

## 3.7 数据访问层 (DAO)

**源文件**：`data/dao.py`

为消除路由层重复的数据库操作代码，系统引入 DAO（Data Access Object）模式：

| DAO | 职责 | 核心方法 |
|-----|------|---------|
| `ResourceDAO` | 学习资源 CRUD | save, get_by_user, count_by_user, delete |
| `ActivityDAO` | 活动日志管理 | record, get_recent, count_by_user, get_login_days |

**设计优势**：
- 统一使用 `with` 上下文管理器管理数据库连接，杜绝连接泄漏
- 路由层不再直接操作 SQL，业务逻辑与数据访问解耦
- 全局单例延迟初始化，避免循环导入

## 3.8 自定义异常体系

**源文件**：`backend/exceptions.py`

系统定义 8 种业务异常，实现分层异常处理：

| 异常类 | HTTP 状态码 | 用途 |
|--------|-----------|------|
| `ValidationError` | 400 | 请求参数校验失败 |
| `AuthenticationError` | 401 | 认证失败 |
| `AuthorizationError` | 403 | 权限不足 |
| `RateLimitError` | 429 | 请求过于频繁 |
| `DatabaseError` | 500 | 数据库操作异常 |
| `AIServiceError` | 502 | AI 服务调用失败 |
| `ResourceGenerationError` | 500 | 资源生成异常 |
| `AppException` | 可配置 | 业务基础异常 |

全局异常处理器自动区分日志级别：5xx 记录 error，4xx 记录 warning。

---

# 第四部分 前端工程

## 4.1 技术选型与架构

| 层 | 技术 | 说明 |
|----|------|------|
| 框架 | Next.js 12 | Pages Router + React 17 |
| 样式 | Tailwind CSS | 原子化 CSS |
| 动画 | Framer Motion 6 | 流畅交互动效 |
| 状态 | Zustand | 轻量级状态管理 |

## 4.2 六大功能模块

| 模块 | 功能 | 核心组件 |
|------|------|---------|
| 学生画像 | 对话式画像 + 课程表 + 成绩 + 错题 | ProfileModule |
| 资源生成 | 7 种类型一键生成 | ResourcesModule |
| 学习路径 | AI 路径规划 + 进度跟踪 | PathModule |
| 智能辅导 | 多轮对话 + Mermaid 图解 | TutorModule |
| 效果评估 | 多维度评估报告 | AssessmentModule |
| 知识库 | 文档上传 + RAG 检索 | RagKnowledgeModule |

---

# 第五部分 企业级特性

## 5.1 安全

| 特性 | 说明 |
|------|------|
| JWT 认证 | HS256，access + refresh token 分离，启动时强制校验密钥 |
| 速率限制 | 全局 120/min，登录 10/min，AI 端点独立限流 |
| 请求校验 | Pydantic 模型强制参数校验，非法参数直接拒绝 |
| 异常分层 | 自定义异常类（8 种），业务异常与系统异常分离处理 |
| 安全头 | 7 个 HTTP 安全头 + CSP |
| CORS | 环境变量白名单 |
| XSS 防护 | rehype-sanitize |
| SQL 注入 | 全部参数化查询 |

## 5.2 可观测性

| 特性 | 说明 |
|------|------|
| 结构化日志 | JSON 格式，轮转 10MB/30天 |
| 请求追踪 | X-Request-ID 全链路（自动生成 8 位短 ID） |
| 耗时统计 | X-Response-Time |
| 健康检查 | `/api/health` 返回 SQLite/FAISS 状态 |

## 5.3 容器化

| 特性 | 说明 |
|------|------|
| Docker | 前后端独立镜像，非 root |
| docker-compose | 一键启动（后端+前端+SQLite） |
| 健康检查 | 容器级健康检查，自动重启 |

## 5.4 启动方式

```bash
# 方式一：一键启动
双击 启动.bat

# 方式二：手动启动
# 后端
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000

# 前端
cd frontend && npm run dev

# 方式三：Docker
docker-compose up -d
```

访问地址：
- 前端：http://localhost:3000
- 后端：http://localhost:8000
- API 文档：http://localhost:8000/api/docs（DEBUG 模式）

## 5.5 打包与分发

### 5.5.1 打包为 EXE 安装包

系统支持使用 PyInstaller + NSIS 打包为 Windows 安装程序，实现一键安装部署。

#### 打包流程

```
┌─────────────────────────────────────────────────────────────┐
│                    打包构建流程                               │
├─────────────────────────────────────────────────────────────┤
│  1. 构建前端 (npm run build)                                 │
│     ↓                                                        │
│  2. 下载便携 Node.js / SQLite                                 │
│     ↓                                                        │
│  3. PyInstaller 打包后端为 EXE                               │
│     ↓                                                        │
│  4. 组装分发目录 (dist/AI学习智能体/)                          │
│     ↓                                                        │
│  5. NSIS 编译安装程序 (AI学习智能体_Setup.exe)                  │
└─────────────────────────────────────────────────────────────┘
```

#### 使用构建脚本

```bash
# 一键构建（推荐）
python build.py
```

构建脚本自动完成以下步骤：
- 检查并安装 PyInstaller 依赖
- 构建前端 standalone 版本
- 下载便携版 Node.js 和 SQLite
- 运行 PyInstaller 打包后端
- 组装完整的分发目录

#### 使用 NSIS 编译安装包

```bash
# 编译 NSIS 安装程序
makensis installer.nsi
```

生成文件：
| 文件 | 大小 | 说明 |
|------|------|------|
| `AI学习智能体_Setup.exe` | ~340 MB | 完整安装程序 |
| `dist/AI学习智能体/` | ~1.2 GB | 免安装版本 |

#### 安装包特性

| 特性 | 说明 |
|------|------|
| 安装向导 | 标准 Windows 安装界面 |
| 桌面快捷方式 | 可选创建 |
| 开始菜单 | 自动创建程序组 |
| 注册表写入 | 支持程序卸载 |
| 环境检测 | 自动检查端口和服务状态 |
| 一键启动 | 安装后可直接启动 |

#### 打包产物结构

```
dist/AI学习智能体/
├── AI学习智能体.exe      # 主程序（PyInstaller 打包）
├── _internal/            # Python 运行时依赖
├── frontend/             # Next.js 前端（standalone）
├── node/                 # 便携 Node.js
├── data/databases/       # SQLite 数据库文件
├── config/               # 配置文件
├── scripts/              # 脚本文件
├── data/                 # 数据文件
└── .env.example          # 环境变量示例
```

---

# 附录

## 附录A：技术栈清单

| 层 | 技术 | 版本 | 用途 |
|----|------|------|------|
| 前端框架 | Next.js | 14.2+ | Pages Router + React 17 |
| 前端样式 | Tailwind CSS | 3.4+ | 原子化CSS |
| 前端动画 | Framer Motion | 11.0+ | 流畅交互动效 |
| 前端状态 | Zustand | 4.5+ | 轻量级状态管理 |
| 后端框架 | FastAPI | 0.110+ | RESTful API + SSE |
| Python | Python | 3.8+ | 后端运行环境 |
| 数据库 | SQLite | 3.50+ | 9个独立.db文件（内置sqlite3模块） |
| 向量索引 | FAISS | 1.7+ | ANN近似最近邻 |
| AI模型 | MiMo AI | mimo-v2.5-pro | 对话/推理/多模态/视觉/图片生成/语音合成 |
| 向量化 | MiMo Embedding | 768维 | 文本向量化（mimo-embedding） |
| 容器化 | Docker | 24+ | 前后端独立镜像 |

## 附录B：API接口完整列表

### 认证接口 `/api/auth`
| 方法 | 端点 | 功能 |
|------|------|------|
| POST | /login | 用户登录 |
| POST | /register | 用户注册 |
| GET | /me | 获取当前用户 |
| POST | /change-password | 修改密码 |
| POST | /guest | 游客模式 |

### 智能体接口 `/api/agent`
| 方法 | 端点 | 功能 |
|------|------|------|
| POST | /build-profile | 构建学生画像 |
| GET | /get-profile | 获取画像 |
| POST | /update-profile-field | 更新画像字段 |
| POST | /generate-resources | 生成资源 |
| POST | /export-resource | 导出资源 |
| POST | /plan-path | 规划路径 |
| POST | /update-path-progress | 更新进度 |
| POST | /tutor | 智能辅导 |
| POST | /assess | 效果评估 |
| POST | /comprehensive-plan | 综合计划 |
| GET | /dashboard/stats | 工作台统计 |
| GET | /activity-logs | 活动日志 |
| POST | /advanced-search | 高级检索（统一入口） |
| POST | /hyde-search | HyDE检索 |
| POST | /multi-query-search | 多查询检索 |
| POST | /rag-fusion-search | RAG-Fusion检索 |
| POST | /graph-search | 图谱增强检索 |
| POST | /contextual-upload | 上下文分块上传 |
| POST | /upload-to-rag | 上传到RAG |
| GET | /rag-documents | RAG文档列表 |
| POST | /save-course-schedule | 保存课程表 |
| GET | /get-course-schedule | 获取课程表 |
| POST | /save-grades | 保存成绩 |
| GET | /get-grades | 获取成绩 |
| POST | /save-error-note | 保存错题 |
| GET | /get-error-notes | 获取错题 |
| POST | /generate-study-plan | 生成学习计划 |
| GET | /get-study-plans | 获取学习计划 |

### 流式接口 `/api/stream`
| 方法 | 端点 | 功能 |
|------|------|------|
| GET | /generate-resources-real | SSE资源生成 |
| POST | /tutor | SSE智能辅导 |
| POST | /safety-check | 安全检查 |
| POST | /fact-verify | 事实验证 |

## 附录C：数据库表结构

### ai_auth (认证库)
- `users` - 用户表
- `sessions` - 会话表

### ai_profiles (画像库)
- `student_profiles` - 学生画像（9维度JSON）
- `course_schedules` - 课程表
- `student_grades` - 成绩
- `error_notes` - 错题本
- `study_plans` - 学习计划

### ai_resources (资源库)
- `learning_resources` - 学习资源
- `safety_logs` - 安全日志

### ai_paths (路径库)
- `learning_paths` - 学习路径
- `path_progress` - 路径进度

### ai_tutor (辅导库)
- `tutor_sessions` - 辅导会话
- `tutor_messages` - 消息记录
- `tutor_knowledge_refs` - 知识引用

### ai_assessments (评估库)
- `learning_assessments` - 评估记录
- `assessment_dimensions` - 评估维度
- `learning_activities` - 学习活动

### ai_agents (协作库)
- `collaboration_logs` - 协作日志
- `agent_tasks` - 任务记录

### ai_rag_knowledge (知识库)
- `knowledge_documents` - 知识文档（含768维向量，FTS5全文索引）
- `knowledge_points` - 知识点
- `document_categories` - 文档分类
- `document_relations` - 文档关联
- `usage_logs` - 使用日志

### ai_memory (记忆库)
- `short_term_memory` - 短期记忆
- `episodic_memory` - 情景记忆
- `semantic_memory` - 语义记忆（SPO三元组）
- `entity_memory` - 实体记忆（KV存储）
- `entity_relations` - 实体关系（知识图谱）
- `memory_metadata` - 记忆元数据（遗忘控制）
- `memory_conflicts` - 记忆冲突
- `memory_access_log` - 访问日志

## 附录D：环境变量配置

| 变量 | 说明 | 默认值 |
|------|------|--------|
| JWT_SECRET | JWT签名密钥（必须配置，缺失时启动失败） | 必填 |
| DEBUG | 调试模式 | false |
| APP_VERSION | 应用版本 | 1.0.0 |
| ALLOWED_ORIGINS | CORS白名单 | localhost:3000 |
| MIMO_API_KEY | MiMo API 密钥 | 必填 |
| MIMO_BASE_URL | MiMo API 地址 | https://api.mimo.ai/v1 |
| MIMO_MODEL | 推理模型 | mimo-v2.5-pro |
| SPARK_BASE_URL | 对话API地址 | spark-api-open.xf-yun.com |
| SPARK_MODEL | 对话模型 | mimo-v2.5-pro |
| SPARK_EMBEDDING_URL | Embedding地址 | emb-cn-huabei-1.xf-yun.com |
| LOG_LEVEL | 日志级别 | INFO |
| MAX_UPLOAD_SIZE | 上传限制(MB) | 50 |

---

## 附录E：v7.3 重构变更清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `backend/exceptions.py` | 8 种自定义异常类，分层异常处理 |
| `data/dao.py` | 数据访问对象层（ResourceDAO + ActivityDAO） |
| `backend/schemas/request_models.py` | 12 个 Pydantic 请求模型 |
| `config/sensitive_words.json` | 敏感词外部配置（125 词） |

### 修改文件

| 文件 | 变更 |
|------|------|
| `backend/dependencies.py` | JWT 密钥缺失时启动失败（不再静默降级） |
| `backend/main.py` | 导入自定义异常模块，新增统一异常处理器 |
| `backend/api/agent.py` | 核心端点改用 Pydantic 模型 + DAO 层，消除约 80 行重复代码 |
| `backend/api/stream.py` | 新增速率限制器导入，为 AI 端点添加限流基础 |
| `services/spark_client.py` | 日志中隐藏 API 密钥前缀 |
| `services/streaming_service.py` | ProgressTracker 新增 force_cleanup()，防止内存泄漏 |
| `services/content_safety_service.py` | 敏感词支持外部 JSON 配置热更新 |
| `系统开发说明书.md` | 同步更新文档至 v7.3 |
